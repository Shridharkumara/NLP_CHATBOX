{
  "name": "neuralchat-server",
  "version": "1.0.0",
  "description": "NeuralChat backend - keeps the Gemini API key server-side so the chatbot works for any visitor without them providing a key",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "dotenv": "^16.4.5",
    "express-rate-limit": "^7.4.0",
    "cors": "^2.8.5"
  }
}
